<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions should be declared here, outside the <application> tag -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Baitap1"
        tools:targetApi="31">
        <activity
 code-SoDo
            android:name=".SpendingChartActivity"
            android:exported="false" />
        <activity
            android:name=".RegisterActivity"
            android:exported="false" /> <!-- LoginActivity là màn hình chạy đầu tiên -->
        <activity
 main
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- MainActivity không còn là launcher -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <activity
            android:name=".AppDataActivity"
            android:exported="false" />
        <activity
            android:name=".editTask"
            android:exported="false" />
        <activity
            android:name=".CreateNewTaskActivity"
            android:exported="false" />
        <activity
            android:name=".StatisticsActivity"
            android:exported="false" />
 feature/giong-noi

        <activity
            android:name=".RegisterActivity"
            android:exported="false" />

        <!-- THÊM KHAI BÁO MỚI TẠI ĐÂY -->
        <activity
            android:name=".AddExpenseByVoiceActivity"
            android:exported="false" />


code-SoDo
<activity
            android:name=".RegisterActivity"
            android:exported="false" />

 feature/login-ui-update
        <activity android:name=".AddTransactionActivity"
            android:exported="false"
            android:label="Thêm Giao dịch"/>

        <activity android:name=".BudgetActivity"
            android:exported="false"
            android:label="Kế hoạch Ngân sách"/>

        <activity android:name=".AddBudgetActivity"
            android:exported="false"
            android:label="Thêm Ngân sách"/>

main
 main
 main
    </application>

</manifest>
